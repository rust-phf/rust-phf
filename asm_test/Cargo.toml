[package]
name = "asm_test"
version = "0.1.0"
edition = "2021"

[dependencies]
phf = { path = "../phf", features = ["macros"] }

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "phf_lookup"
harness = false

[features]
ptrhash = ["phf/ptrhash"]

