[licenses]
confidence-threshold = 0.90
allow = [
    "Apache-2.0",
    "MIT",
    "Unicode-3.0"
]
private = { ignore = true }

[bans]
multiple-versions = "deny"
skip = ["syn@2"]

[bans.build]
executables = "deny"
